#### NOTE In Lab 5 (offline lab), before running the last CI-CD pipeline (for MLP), please replace "de2024-434212" in diabetes_predictor_mlp.yaml with your project ID or use your own pipeline YAML file (compiled from the notebook). Remember to push changes to the YAML files to your GitHub repository before running the CI-CD pipeline.

